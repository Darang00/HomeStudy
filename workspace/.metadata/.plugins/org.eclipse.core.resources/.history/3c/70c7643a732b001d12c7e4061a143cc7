package sec01.exam01;

public class StudentExample { // 실행 클래스 > 라이브러리 클래스를 참조하여 프로그램의 실행 진입점인 main() 메소드를 제공하는 역할을 한다.

	public static void main(String[] args) {
		Student s1 = new Student();
		s1.speaking(s1);
		
		
		Student s2 = new Student();
		s2.speaking(s2);
		

	}

}
